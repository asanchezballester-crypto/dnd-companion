const fs = require('fs'); eval(fs.readFileSync('spells_db.js', 'utf8')); const query = ''; const selectedLvl = 'all'; const selectedClass = 'all'; const onlyRituals = false; const filteredDbSpells = SPELLS_DB_ES.filter(spell => { const matchesQuery = spell.name.toLowerCase().includes(query) || spell.description.toLowerCase().includes(query); const matchesLvl = selectedLvl === 'all' || spell.level === parseInt(selectedLvl); const matchesClass = selectedClass === 'all' || spell.class.includes(selectedClass); const isRitual = spell.castTime && spell.castTime.toLowerCase().includes('ritual'); const matchesRitual = !onlyRituals || isRitual; return matchesQuery && matchesLvl && matchesClass && matchesRitual; }); console.log('Filtered spells length:', filteredDbSpells.length);
